export const OG_SITE_NAME = "Deploybase";

// Page-level openGraph objects replace the root layout's, so every page that
// sets openGraph must repeat siteName and the sized image.
export const OG_IMAGE = {
  url: "/assets/og-image.png",
  width: 1200,
  height: 630,
} as const;
